<div class="container-fluid">
  <div class="row flex-nowrap">
    <%= render 'admin/shared/side_nav' %>

    <div class="col p-0">
      <%= render 'admin/shared/nav' %>

      <div class="container-fluid w-25 mt-4">
        <h1>Add New Offer</h1>
        <div class="card shadow p-4">
          <%= render partial: 'admin/offers/shared/form', locals: { path: offer_path } %>
        </div>
      </div>

    </div>
  </div>
</div>